#!/bin/bash -

if [ $1 ]; then
    textql -header -sql "SELECT * where year = $(date +%Y) ORDER BY level" $1
else
    echo "$0 FILE.csv"
    echo "FILE can be generated by 'download-best-papers > FILE.csv'"
fi
