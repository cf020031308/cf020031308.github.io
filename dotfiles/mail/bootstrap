#!/bin/bash -

PUBLIC="$PWD"
PRIVATE="$HOME/Nutstore/mail"

ln -sf $PUBLIC ~/.mail
ln -sf $PUBLIC/muttrc ~/.muttrc
ln -sf $PUBLIC/offlineimaprc ~/.offlineimaprc
ln -sf $PRIVATE/msmtprc ~/.msmtprc


brew install offlineimap msmtp mutt lynx
