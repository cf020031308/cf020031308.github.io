set folder = ~/Mail/cf020031308@163.com
set spoolfile = +INBOX
mailboxes +INBOX +Archive +spams +deleted +sent +ads +subs +\&dcVr0mWHTvZZOQ- +drafts +push

set mbox_type = Maildir
set sort = threads
set pager_stop
set tilde
set nomarkers
set delete

set realname = "Roy"
set from = "cf020031308@163.com"
set sendmail = "/usr/local/bin/msmtp -a cf020031308"
unset record

set mailcap_path = ~/.mail/mailcap
set alias_file = ~/Nutstore/muttalias
source ~/Nutstore/muttalias
